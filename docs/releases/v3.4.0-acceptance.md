# 3.4.0 verification

Local Ubuntu verification before tagging:

- 179 backend tests passed.
- 101 frontend unit tests passed.
- Production frontend build and version consistency checks passed.
- All 44 applicable browser cases passed: 42 in the full run, with two updated color expectations passing on focused rerun. One optional private-video benchmark was skipped.
- Visually inspected frame 2300, compact toolbar shortcut hints, and removal of redundant controls in the running local app.
- Separate temporary data directories were used for annotation tests; user project data was backed up before restarting the local service.

The tagged release workflow repeats the full suite and gates publication on Windows installer/runtime verification, Debian installed-runtime verification, and source-license packaging. Successful releases include the machine-readable verification reports beside the installers.

Windows validation uses a native CI runner; Debian validation uses a headless container. This is not a physical Windows 11 laptop acceptance test.
